# VK PR Helper — Web Frontend

React 19 + Vite 6 + VKUI frontend for the VK PR Helper service.

## Deployment

Frontend is deployed to Yandex Object Storage static website hosting. Build with \pnpm build\ and sync via \deploy-yc.cjs\.
